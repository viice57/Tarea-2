  /*
  // LINEAS APRA RECORRER UN MAPA
  Pair* valor = firstMap(precio);
  printf("Dentro del Mapa de ciudades\n");
  while(valor!=NULL){
    printf("%s: %s\n", (char*) valor->key, (char *) valor->value);
    valor = nextMap(precio);
  }*/

/* prototipo
property * searchProperty(int id, char sType) {
  property * newProperty = (property *) malloc(sizeof(property));
  return newProperty;
}
*/